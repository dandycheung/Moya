version: 2.1
jobs:
  build:
    docker:
      - image: cimg/base:current
    steps:
      - run: echo "CI moved to GitHub Actions"
  carthage_without_swiftlint_integration:
    docker:
      - image: cimg/base:current
    steps:
      - run: echo "CI moved to GitHub Actions"
workflows:
  version: 2
  pr_build:
    jobs:
      - build
      - carthage_without_swiftlint_integration
